name: Action
on: push

jobs:
  build_executable:
    runs-on: ubuntu:18.04
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-go@v2
        with:
          go-version: '1.15.0'
      - run: tools/build.sh